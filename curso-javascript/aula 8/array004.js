const number = [1, 2, 4, 5];

number.pop(); // Remove last element of array.
console.log(number);

number.shift(); // Remove first element of array.
console.log(number);

number.splice(1, 1); // Remove element referent is paramater.
console.log(number);
