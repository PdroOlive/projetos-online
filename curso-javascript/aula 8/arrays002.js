const number = [1, 3, 4, 2, 4];

console.log(number.indexOf(4)); // indexOf return index of element in array.
console.log(number.lastIndexOf(4)); // LastIndexOf return index of last element in array, if elements is equality
console.log(number.includes(3)); //includes verify if array contains is element of paramater, if yes return true, else false.